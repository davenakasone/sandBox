/*
    represents the message parser in action
    ...assume message correctly parsed and data got put in "Location" struct
*/

#ifndef H_stateMachine
#define H_stateMachine

void* runStateMachine(void* arg);
//void runStateMachine(Location* ptr);

#endif

////////~~~~~~~~END>  stateMachine.h
