https://pypi.org/project/Flask/
https://flask.palletsprojects.com/en/2.2.x/installation/
https://jinja.palletsprojects.com/en/3.1.x/
https://werkzeug.palletsprojects.com/en/2.2.x/
https://wsgi.readthedocs.io/en/latest/

Jinja is a template engine (comes with MarkupSafe to avoid injection attacks)
Werkzeug is a WSGI (web server gateway interface) toolkit
Flask should be simple + used some important insights from Django
there are a lot of other dependencies, but it is fairly lite

reset the environment to your app everytime
    $ FLASK_APP=app.py flask run

$ sudo lsof -i -P | grep LISTEN | grep 5000
    If ControlCe is returned (Apple's Control Center) is running. To turn it OFF go to:
        Apple/System Preferences/Sharing/ and turn OFF AirPlay Receiver

chrome:
settings > clear browsing data > (only cached images and files form last hour...)